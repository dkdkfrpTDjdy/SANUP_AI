services:
  dashboard-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dashboard-pipeline-app
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
